# Only buildings.json is too large to commit (61MB); it is served from object
# storage via window.ATLAS_BUILDINGS_URL. The OSM context layers are small and tracked.
data/buildings.json
data/*_fetch_progress.json

# Python / env
__pycache__/
*.pyc
venv/
.venv/

# Local tooling / OS cruft
.claude/
.DS_Store
Thumbs.db

# docs / prototype
docs/

# Working documents — session handoffs, notes, plans, reports. They are written for
# one session and go stale immediately, so they stay local. README is the one document
# the repo keeps; LICENSE.txt is not ignored because only *.md is.
*.md
!README.md

# data preparation
# scripts/

# deploy cruft
data/buildings_fetch_progress.json
